#ifndef MOD_H
#define MOD_H

/* Минимальный интерфейс модуля - обязательные функции */

/* Инициализация модуля (обязательная функция)
 * Вызывается при загрузке модуля
 * Возвращает 0 при успехе, отрицательное значение при ошибке
 */
int mod_init(void);

/* Деинициализация модуля (обязательная функция)
 * Вызывается при выгрузке модуля
 */
void mod_fini(void);

/* Экспортируемая функция модуля (опциональная) */
void mod_hello(void);

#endif /* MOD_H */

